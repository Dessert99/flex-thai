/** 관리자 어휘 전체 교체 form을 공개 요청 shape로 검증한다 */
import { adminVocabularyReplaceRequestSchema } from '@flex-thia/contracts';

/** RHF가 사용하는 어휘 교체 schema */
export const vocabularyFormSchema = adminVocabularyReplaceRequestSchema;
