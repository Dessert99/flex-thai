{
  "name": "@flex-thia/config",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --root ../.. backend/config/src",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^4.1.0"
  }
}
