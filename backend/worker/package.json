{
  "name": "@flex-thia/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --root ../.. backend/worker/src",
    "build": "tsc -p tsconfig.json",
    "build:lambda": "node esbuild.config.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.900.0",
    "@aws-sdk/client-sfn": "^3.900.0",
    "@flex-thia/database": "workspace:*",
    "@flex-thia/domain": "workspace:*",
    "@flex-thia/providers": "workspace:*"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "esbuild": "^0.25.0",
    "tsx": "^4.20.0"
  }
}
