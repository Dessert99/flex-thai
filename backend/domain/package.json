{
  "name": "@flex-thia/domain",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --root ../.. backend/domain/src",
    "build": "tsc -p tsconfig.json"
  }
}
