# 태국어 학습 및 문제 서비스

- 태국어를 효율적으로 학습하여 FLEX 시험을 빠른 시일 내에 통과할 수 있도록 돕는 서비스


## 기술 스택
- 프론트엔드: VITE, tailwind css, shadcn, tanstack query
- 백엔드: NestJS



## 코딩 행동 지침

LLM이 코딩할 때 흔히 저지르는 실수를 줄이기 위한 행동 지침. 필요에 따라 프로젝트별 지침과 병합해 사용한다.

**트레이드오프:** 이 지침은 속도보다 신중함 쪽으로 치우쳐 있다. 사소한 작업에는 재량껏 판단한다.

### 1. 코딩하기 전에 생각하라

**추측하지 마라. 혼란을 숨기지 마라. 트레이드오프를 드러내라.**

구현하기 전에:

- 가정을 명시적으로 밝혀라. 확신이 없으면 물어라.
- 해석이 여러 개라면 모두 제시하라 — 말없이 하나를 고르지 마라.
- 더 단순한 방법이 있으면 말하라. 정당한 근거가 있으면 반박하라.
- 불분명한 게 있으면 멈춰라. 무엇이 헷갈리는지 짚어라. 물어라.

### 2. 단순함을 우선하라

**문제를 푸는 최소한의 코드. 추측성 작업은 없다.**

- 요청받은 것 이상의 기능은 없다.
- 한 번 쓰는 코드에 추상화는 없다.
- 요청하지 않은 "유연성"이나 "설정 가능성"은 없다.
- 불가능한 시나리오를 위한 에러 처리는 없다.
- 200줄을 썼는데 50줄로 가능하면, 다시 써라.

스스로 물어라: "시니어 엔지니어가 이걸 과하게 복잡하다고 할까?" 그렇다면, 단순화하라.

### 3. 외과적으로 변경하라

**꼭 필요한 것만 건드려라. 네가 만든 것만 치워라.**

기존 코드를 수정할 때:

- 인접한 코드·주석·포맷을 "개선"하지 마라.
- 고장 나지 않은 것을 리팩터링하지 마라.
- 네 방식과 다르더라도 기존 스타일을 따라라.
- 관련 없는 죽은 코드를 발견하면 언급만 하라 — 지우지 마라.

네 변경이 고아(orphan) 코드를 만들면:

- 네 변경 때문에 안 쓰이게 된 import·변수·함수를 제거하라.
- 원래 있던 죽은 코드는 요청받지 않는 한 제거하지 마라.

기준: 바뀐 모든 줄은 사용자의 요청으로 곧장 추적될 수 있어야 한다.

### 4. 목표 주도로 실행하라

**성공 기준을 정의하라. 검증될 때까지 반복하라.**

작업을 검증 가능한 목표로 바꿔라:

- "검증을 추가하라" → "잘못된 입력에 대한 테스트를 작성하고, 그 테스트를 통과시켜라"
- "버그를 고쳐라" → "그 버그를 재현하는 테스트를 작성하고, 통과시켜라"
- "X를 리팩터링하라" → "전후로 테스트가 통과하는지 확인하라"

여러 단계로 이뤄진 작업은 간단한 계획을 밝혀라:

```
1. [단계] → 검증: [확인]
2. [단계] → 검증: [확인]
3. [단계] → 검증: [확인]
```

강한 성공 기준은 스스로 반복하며 일하게 해준다. 약한 기준("그냥 되게 만들어")은 끊임없는 재확인을 요구한다.

### 5. 주석 컨벤션은 기본 적용한다

모든 새 코드와 변경되는 코드는 `conventions/comment-convention.md`를 따른다.

- 사용자가 따로 지시하지 않아도 적용한다.
- 새 파일에는 파일 헤더 주석을 둔다.
- 새로 추가하거나 수정하는 export에는 한 줄 JSDoc을 둔다.
- 함수 내부에는 비자명한 의도·함정에만 한 줄 `//` 주석을 둔다.
- 기존 파일 전체에 소급 적용하지 않고, 이번 작업에서 건드리는 범위에만 적용한다.

### 6. 테스트 설명은 한국어로 작성한다

Vitest/Jest 계열 테스트의 `describe`, `it`, `test` 설명 문자열은 한국어로 쓴다.

- 테스트 이름은 사용자가 읽는 동작 설명이므로 영어 문장으로 남기지 않는다.
- 코드 식별자가 필요하면 설명 안에 식별자를 섞되, 문장 자체는 한국어로 쓴다.
- 기존 테스트를 수정할 때는 해당 파일의 `describe`, `it`, `test` 설명 문자열도 함께 한국어로 맞춘다.

### 7. 프로젝트 구조와 프론트엔드 컨벤션

코드 위치와 의존성 방향은 `conventions/structure-convention.md`를 단일
기준으로 따른다. 프론트엔드 컴포넌트 작업은
`conventions/frontend/component-convention.md`도 함께 따른다.

### 8. 백엔드 아키텍처

백엔드 작업은 `conventions/structure-convention.md`와
`docs/development/backend-architecture.md`를 작업 전에 읽고 따른다.
경로 목록과 배치 규칙은 이 문서에 복제하지 않는다.

### 9. E2E 테스트는 두지 않는다

이 프로젝트는 E2E 테스트(브라우저·API 통합 포함)를 추가하지 않는다.

- Playwright 등 E2E 러너·설정·스펙을 새로 만들지 않는다.
- 검증은 단위/컴포넌트 테스트(Vitest)와 lint·typecheck로 한다.
- 사용자가 명시적으로 요청하지 않는 한 E2E 스캐폴딩을 제안하거나 도입하지 않는다.

---

**이 지침이 잘 작동하고 있다는 신호:** diff에 불필요한 변경이 줄고, 과한 복잡함 때문에 다시 쓰는 일이 줄며, 명확화 질문이 실수 뒤가 아니라 구현 전에 나온다.


절대 시키지도 않았는데 PR, 브랜치를 만들지 않는다.
